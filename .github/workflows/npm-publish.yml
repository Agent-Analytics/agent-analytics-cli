name: NPM Publish (disabled pending npm support)

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Disabled pending npm support resolution
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is disabled
        run: |
          echo "Trusted publishing for @agent-analytics/cli is currently disabled."
          echo "npm returns E404 for this package even after trusted publisher setup was verified."
          echo "Use manual npm publish with OTP from main until npm support resolves the issue."
          echo "See docs/RELEASE_SETUP.md for the current release process."
          exit 1
